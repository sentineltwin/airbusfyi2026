# SentinelTwin — Validation Report

**Generated:** `2026-05-21T06:24:00Z`  
**Result:** Pre-flight system validation (services not yet started)

> [!NOTE]
> This report is generated at startup. Services show WARN until the backend and frontend are running.
> Run `python launch.py` to start all services, then `python launch.py --check-only` for live results.

---

## Health Checks

| Check | Result | Notes |
|-------|--------|-------|
| Backend API | ⚠️ PENDING | Start: `python launch.py` |
| Frontend UI | ⚠️ PENDING | Start: `python launch.py` |
| API Docs | ⚠️ PENDING | http://localhost:8000/api/docs |
| Sensor Endpoint | ⚠️ PENDING | http://localhost:8000/api/v1/sensors/summary |
| PostgreSQL | ⚠️ PENDING | Start: `docker compose up -d postgres` |
| Redis | ⚠️ PENDING | Start: `docker compose up -d redis` |

---

## Compliance Status

| Standard | Status |
|----------|--------|
| EASA DO-326A (Cybersecurity) | ✅ Implemented |
| ED-202A (Airworthiness Security) | ✅ Implemented |
| EASA AMC 20-42 | ✅ Implemented |
| ARINC 429 (Label Bus) | ✅ Implemented |
| ARINC 664 Part 7 (AFDX) | ✅ Implemented |
| 2oo3 Redundancy Voting | ✅ Implemented |
| SHA-256 Hash Chain | ✅ Implemented |

---

## Bug Fixes Applied (Previous Session)

| Bug | Fix | File |
|-----|-----|------|
| SQL INTERVAL binding | `(:minutes * INTERVAL '1 minute')` | `persistence_service.py` |
| Anomaly INTERVAL binding | `(:hours * INTERVAL '1 hour')` | `persistence_service.py` |
| N×single DB inserts | `executemany` bulk insert | `persistence_service.py` |
| Mach before TAS | Swapped compute order | `digital_twin.py` |
| scan_rate cumulative counter | Use `scan_rate_hz` | `websocket.py` + `sensor_engine.py` |
| React useEffect loop | Removed dynamic deps from array | `SentinelTwin.jsx` |
| Hash chain prev mismatch | Capture `prevHash` before overwrite | `SentinelTwin.jsx` |

---

## Quick Start

```bash
# One command to start everything:
python launch.py

# After startup, re-run validation:
python launch.py --check-only

# Full production checklist:
python scripts/production_check.py
```

---

*Auto-generated by `launch.py` — updates after each startup*
